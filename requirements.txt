# CPU REQUIREMENTS - For local development, evaluation scripts, plotting, HF push
#
# INSTALLATION:
#   python3 -m venv venv_CITA && source venv_CITA/bin/activate
#   pip install -r requirements.txt
#
# For GPU training, ALSO install: pip install -r requirements_gpu.txt

# Core dependencies
numpy>=1.26.0
pandas==2.3.3
networkx
matplotlib
seaborn
scikit-learn
adjustText
Pillow>=10.0.0
requests
cairosvg

# Jupyter kernel support
ipykernel
debugpy

# Authentication
python-dotenv
huggingface_hub

# LLM-as-judge evaluation (Fireworks API)
litellm
sentence-transformers

# Hyperparameter optimization
optuna==4.1.0

# Logging
wandb==0.22.2

# Linting / static analysis (per CLAUDE.md §6.1: py_compile + AST + ruff)
ruff==0.7.4
