# MINIMAL REQUIREMENTS - For CITA Training
#
# INSTALLATION MODES:
# 1. Lambda A6000 with system packages: python3 -m venv --system-site-packages venv_CITA
#    (Uses system torch 2.7.0, keras 3.10.0 - faster, saves space)
# 2. Fresh GPU instance: python3 -m venv venv_CITA && pip install torch --index-url https://download.pytorch.org/whl/cu121
#    (Installs everything from scratch)

# Core dependencies
numpy<2.0.0  # CRITICAL: System packages (scipy, torch) compiled against NumPy 1.x
pandas>=2.0.0  # Ray requires pandas 2.0+
networkx  # Avoid np.int deprecation errors
matplotlib
seaborn
scikit-learn

# Flash-attention (30-40% speedup, ~2.5GB memory savings)
flash-attn  # Install with: MAX_JOBS=4 pip install flash-attn --no-build-isolation
ninja  # Build dependency (if compiling from source)

# Core fine-tuning packages (essential)
transformers>=4.55.0  # Min version with required features
trl>=0.22.0           # Min version with CITATrainer support (if custom)
tf-keras              # Required until transformers supports System's Keras 3

peft
accelerate
# bitsandbytes  # Not needed - using adamw_torch instead of adamw_8bit
datasets
# unsloth
# jinja2>=3.1.0  # SKIP - system provides via dependencies

# Jupyter kernel support
ipykernel
debugpy

# board
wandb

# Population-Based Training (PBT)
ray[tune]>=2.9.0

# authentication
python-dotenv
huggingface_hub

# LLM-as-judge evaluation (Fireworks API)
litellm
