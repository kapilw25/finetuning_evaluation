# MINIMAL REQUIREMENTS - Compatible with Lambda.ai base image
# Do NOT reinstall: torch, torchvision, tensorflow, numpy, pandas (already in base image)

# Fine-tuning specific packages (not in base image)
transformers>=4.30.0
peft>=0.6.0
accelerate>=0.20.0
bitsandbytes>=0.41.0
datasets>=2.10.0
unsloth>=2024.5.8
trl>=0.7.0

# Evaluation metrics (small packages only)
rouge-score>=0.1.2
evaluate>=0.4.0
nltk>=3.8.0

# Inference backend
ollama>=0.1.26

# Database & utilities (lightweight only)
huggingface-hub>=0.20.0
wandb>=0.15.0
gpustat>=1.1.0
tqdm>=4.64.0

# Statistical analysis (if not in base)
pingouin>=0.5.0